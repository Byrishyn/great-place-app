import React from "react"
import { View, Text, StyleSheet } from "react-native"

const PlaceDetailScreen = props => {
    return (
        <View>
            <Text>
                This is the PlaceDetailScreen
            </Text>
        </View>
    )
}

const styles = StyleSheet.create({})

export default PlaceDetailScreen;